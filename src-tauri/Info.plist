<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- The app window captures the webcam via getUserMedia. WebKit's media
       permission path checks for both camera and microphone usage strings, so
       declare both even though we only use video. -->
  <key>NSCameraUsageDescription</key>
  <string>Gesture Guard analyzes your webcam locally to blur the feed when it detects unprofessional gestures. Video never leaves your device.</string>
  <key>NSMicrophoneUsageDescription</key>
  <string>Gesture Guard does not record audio; this entry is required by the system media-permission flow.</string>
</dict>
</plist>
