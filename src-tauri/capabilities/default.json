{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window. The Python engine is spawned from the Rust core (no shell capability needed).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
