# Gesture Guard — Python privacy engine (Python 3.12; MediaPipe has no 3.13 wheels)
# Everything here installs into the LOCAL venv at src-backend/.venv — nothing global.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
websockets>=12.0
opencv-python-headless>=4.9,<5.0
mediapipe==0.10.35
numpy>=1.26,<2.0
pydantic>=2.6,<3.0
# Phase 2: expose the guarded feed as a virtual webcam for Zoom/Teams.
# Needs a system virtual-camera driver at runtime (OBS Virtual Camera, etc.);
# degrades gracefully if none is installed.
pyvirtualcam>=0.11
# Build-time only: freezes the engine into a single-file native sidecar
pyinstaller>=6.6
